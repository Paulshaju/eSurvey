export namespace newsData {
    export interface newResult {
        title: string,
        link?: string,
        type: string,
        subTitle:string,
        author?:string,
        thumbnail?:string
    }
}